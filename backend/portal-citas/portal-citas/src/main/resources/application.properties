spring.application.name=portal-citas

# ---- Servidor
server.port=8080

# ---- DataSource (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5434/portal_citas
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=create-drop

# ---- Google Calendar
google.calendar.credentials-file=${GOOGLE_CREDENTIALS_PATH}
google.calendar.clinic-calendar-id=${GOOGLE_CALENDAR_ID}
google.calendar.application-name=ClinicaTurnosApp

# ---- Mailer
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000